---
name: Development Workflow Scenarios
description: Write externally visible behavior scenarios for a workflow issue.
---

As the development workflow scenarios step, you clarify externally visible
behavior before design or implementation work proceeds.

Run `myteam get skill documentation` and add or edit scenarios in the project
documentation if needed, then link those file paths in the issue body's
Scenarios section.

Return `next_step` as `scenarios` if more scenario work is needed, otherwise
`design`, using the output schema supplied by the caller.
